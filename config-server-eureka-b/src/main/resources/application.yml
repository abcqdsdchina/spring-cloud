eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://admin:admin@eureka-a:8083/eureka
  instance:
    hostname: eureka-b
  server:
    eviction-interval-timer-in-ms: 1000
    enable-self-preservation: false

server:
  port: 8084

spring:
  application:
    name: config-server-eureka-b
  security:
    user:
      name: admin
      password: admin
